name: Cobalt
version: ${project.version}
main: se.fusion1013.plugin.cobalt.Cobalt
api-version: 1.13
authors:
  - Fusion1013
description: The Cobalt Plugin
commands:
  emitter:
    description: Emitter command
    usage: /emitter
    aliases:
      - /emit
      - /em
  scenario:
    description: Scenario command
    usage: /scenario
    aliases:
      - /scen
      - /sc